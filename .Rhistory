context1$attendrt <- (context1$attend / 32)*100
setwd("C:\\ps2\\ps2")
rm(list=ls(all=TRUE))
context1    <- fread('attend.csv')
summary(context1)
library(data.table)
context1    <- fread('attend.csv')
summary(context1)
context1$attendrt <- (context1$attend / 32)*100
context1$hwrt <- (context1$hw / 8)*100
model1 <- lm(termGPA ~ priGPA + ACT + attendrt + hwrt, data = context1)
summary(model1)
